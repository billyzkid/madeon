export const AppTheme = {
  none: "none",
  dark: "dark",
  light: "light"
}

export const AppState = {
  default: "default",
  loading: "loading",
  loaded: "loaded",
  failed: "failed"
}

export const PlayerState = {
  default: "default",
  paused: "paused",
  playing: "playing",
}