export const AppTheme = {
  none: "none",
  madeon: "madeon"
}

export const AppState = {
  default: "default",
  loading: "loading",
  loaded: "loaded",
  failed: "failed"
}

export const PlayerState = {
  default: "default",
  paused: "paused",
  playing: "playing",
}