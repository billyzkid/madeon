export const AppTheme = {
  default: "default"
}

export const AppState = {
  default: "default",
  loading: "loading",
  loaded: "loaded",
  failed: "failed"
}

export const PlayerState = {
  default: "default",
  paused: "paused",
  playing: "playing",
}

export const KeyCodes = {
  escape: 27
};